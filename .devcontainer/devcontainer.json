{
    "name": "Struts 1 Java 1.5",
    "dockerFile": "Dockerfile",
    "runArgs": [
        "--init",
        "--privileged"
    ],
    "overrideCommand": false,
    "customizations": {
        "vscode": {
            "settings": {},
            "extensions": [
                "redhat.java"
            ]
        }
    },
    "postCreateCommand": "mvn clean package",
    "remoteUser": "vscode"
}
